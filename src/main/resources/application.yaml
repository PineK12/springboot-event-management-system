server:
  port: 8080
  servlet:
    session:
      timeout: 30m
      cookie:
        http-only: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vadoo?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: tai123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8

logging:
  level:
    org.springframework.security: DEBUG
    com.vadoo: DEBUG